class Alumno:
    def __init__ (self,calf1,nombre):
        self.__calf1=calf1
        self.__nombre=nombre
        
    def Promedio (self):
        lista.append(self.__calf1)
        if materias==contador:
            suma=sum(lista)
            promedioPersonal=suma/materias
            lista2.append(promedioPersonal)
            print("*"*50)
            print(f"Promedio del Alumno {self.__nombre} :",{promedioPersonal})
            print("*"*50)
            
class Final(Alumno):
    def final(Alumno):
        suma2=sum(lista2)
        return(suma2)